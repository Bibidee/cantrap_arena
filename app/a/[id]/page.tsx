import { notFound } from 'next/navigation';
import { publicClient, readChallenge, addresses } from '@/lib/genlayer/contracts';
export default async function Receipt({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params; const numeric = Number(id); if (!Number.isSafeInteger(numeric) || numeric < 1) notFound();
  let a: any; try { a = await publicClient.readContract({ address: addresses.arena, functionName: 'get_attack', args: [BigInt(numeric)] }); } catch { return <main className="page"><h1>Receipt unavailable</h1><p className="error">No canonical attack exists for #{id}.</p></main>; }
  let c: any = null; try { c = await readChallenge(BigInt(a.challenge_id)); } catch { /* attack state remains useful */ }
  return <main className="page"><p className="eyebrow">CANONICAL ATTACK RECEIPT #{id}</p><h1>Consensus record</h1><div className="spec-grid"><section><label>ATTACKER</label><p>{a.attacker}</p><label>COMMITMENT</label><code>{a.hash}</code><label>EXCERPT</label><pre>{a.excerpt || 'No excerpt recorded.'}</pre><label>EVIDENCE QUOTE</label><p>{a.evidence_quote || 'None'}</p></section><aside><div><label>CHALLENGE</label><b>#{String(a.challenge_id)} {c?.title || ''}</b></div><div><label>COMMITTED</label><b>{new Date(Number(a.committed_at) * 1000).toLocaleString()}</b></div><div><label>REVEALED / TESTED</label><b>{String(a.revealed)} / {String(a.tested)}</b></div><div><label>RESULT</label><b>{a.result || 'PENDING'}</b></div><div><label>SEMANTIC / CLASS</label><b>{a.semantic} / {a.class}</b></div></aside></div><p className="notice">This page shows canonical Arena state. Transaction hashes are shown only where separately persisted; none are invented here.</p></main>;
}
