import Link from 'next/link';
const demos=[{id:1,title:'CANARY VAULT',tag:'FAQ / secret containment',bounty:'12.00 GEN',status:'ACTIVE',expiry:'16h 22m'},{id:2,title:'REFUND CLERK',tag:'policy / false-action claims',bounty:'8.50 GEN',status:'ACTIVE',expiry:'2d 04h'}];
export default function Challenges(){return <main className="page"><header><p className="eyebrow">ACTIVE SPECIMENS / 61999</p><h1>Challenge wall</h1><p>Canonical local templates are shown until a deployed Arena address is configured.</p></header><div className="challenge-wall">{demos.map(x=><Link href={`/x/${x.id}`} className="challenge-card" key={x.id}><div><span className="seal">{x.status}</span><span>#{String(x.id).padStart(3,'0')}</span></div><h2>{x.title}</h2><p>{x.tag}</p><footer><b>{x.bounty}</b><span>expires {x.expiry}</span></footer></Link>)}</div></main>}
